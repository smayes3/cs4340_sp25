import { AbstractMermaidTokenBuilder } from "../common/tokenBuilder.js";

export class ActivityTokenBuilder extends AbstractMermaidTokenBuilder {
    public constructor() {
        super(['activity-beta'])
    }
}